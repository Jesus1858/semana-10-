print("Este codigo esta programado a arrojar el factorial de un numero entero.")
numero = int(input("Digite el numero que desea factorizar:..."))
fact = 1
for i in range(1,numero+1):
    fact = fact * i
print("El resultado es:...",fact)
#FOR:Un bucle for establece la variable iteradora en cada valor de una lista, arreglo 
#o cadena proporcionada y repite el código en el cuerpo del bucle for para cada valor de la variable iteradora
